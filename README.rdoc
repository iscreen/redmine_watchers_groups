= redmine_watchers_groups

Tested on redmine version 4.0.x

For plugin db migration
```
rake redmine:plugins:migrate
```

older redmine version: https://github.com/pstengel/redmine_groups_act_as_watchers
